# LINE-REDUCTION — campaign log

Metric command and binding rules: `REQUIREMENTS.md`. Goal: 100 lines
(aspirational); every entry must be ≤ the previous one.

| When | Lines | Δ | Commit | What changed |
|---|---|---|---|---|
| 2026-09-14 | 3624 | — | (docs-reorg commit) | baseline: UI reviews removed, docs deduplicated into README + docs/, requirements recorded in REQUIREMENTS.md |
| 2026-09-14 | 3461 | -163 | 068dd06 | views.py 963→800: comment/banner trim to one-liners, merged _events/_latest_instance_events, inlined _table_ctx, _asset() factory for the 4 asset views, one-line dispatchers, _edit_page render dedupe; full memory-backend smoke (login→provision→edit→bind→delete) green |
| 2026-09-14 | 3399 | -62 | 891658f | stdlib partial() replaces hand-written wrapper fns (kube list_* ×5, memory delete_* ×2); BIND_SCHEMAS as a comprehension; form_schema_for dead bind branch removed; records.py's two catalog builders merged + dead refName dropped; full flow smoke green |
| 2026-09-14 | 3321 | -78 | 5e521f4 | params_drawer.html shared partial (provision_form/edit_page 11-line drawers → 1 include each); authz _env_tenants dedupe; oidc uses secrets.token_urlsafe (stdlib) for verifier/state; docs/architecture.md 137→85 (descriptive only); README non-binding prose tightened; app.css chip rules merged; full flow smoke green |
| 2026-09-14 | 3297 | -24 | 5ccce59 | docs-only round: design.md rationale/open-proposals/spec-check sections tightened + stale .ws-tab reference removed (all tokens/archetypes/AA values intact); error.html 8→6; public/README .gz row merged; Dockerfile header trim; check+boot smoke+404 render green |
| 2026-09-14 | 3297 | 0 | 7e658d9 | owner review (Musk pass on REQUIREMENTS): tenancy fails closed — FALLBACK_TENANTS explicit-only, the baked-in team-a,team-b default deleted from authz/README; design.md demoted binding→descriptive (binding constraints pinned in REQUIREMENTS §9); tests rule reworded (fix tests, never conform code to them); check + full memory-backend smoke incl. fail-closed authz assertions green |
| 2026-09-14 | 3278 | -19 | 8b638ce | views.py: FormError exception replaces the fail() closures (provision_post/bind_post repaint via one except; _form_params raises instead of returning (params, err)); _dispatch lambda factory replaces the three method dispatchers; form-template comment banners compressed, instance_page delete-forms/Reveal button joined to single lines; full smoke incl. every 400 error path green |
| 2026-09-14 | 3123 | -155 | d7b090b | fan-out round (4 read-only explorer agents): views _safe()/asset() helpers + FormError completed in instance_edit_post + dead context keys; settings drops Django-default lines, manage/wsgi compacted; middleware 500 reuses views._error_page + signed-in guard; input_schema rewritten data-driven (SCHEMAS comprehension, merged _validate, conditional-** props) — 16-section before/after JSON snapshot byte-identical; records next()/comprehensions, oidc _b64url inlined, memory offerings() deleted, authz _env_list inlined, kube slug partials; dead frontend code (app.js get valid(), .tiles rule, htmx-indicator selector, .schema-tools wrapper); identity diff + check + 30/30 smoke green |
| 2026-09-14 | 2683 | -440 | bd62b4d | fan-out round 2 (3 agents, drafts + harnesses under /tmp, independently re-run): kube.py 312→266 rewritten (_ensure/_create_cr/_ref helpers, walrus list_events, partial verbs) — 43/43 stub scenarios old==new incl. call sequences, CR bodies with key order, pinned-params-last; app.js 138→63 + all 14 templates whitespace-joined (layout 43→14) — 133-case smValidateParams battery deep-equal, 14/14 Django compile, structural tag-sequence check; docs deduped vs REQUIREMENTS (README 167→114, design 209→155, architecture 74→59, public/README 44→35, Dockerfile 31→23 with merged COPY, .gitignore 18→12) and REQUIREMENTS.md 105→82 pure re-wrap (normalized token diff identical); identity 16/16 + check + 30/30 smoke green |
| 2026-09-14 | 2377 | -306 | 2c5bd3f | fan-out round 3 (3 agents): views.py 751→648 structural rewrite (_offering_ctx merges provision/edit form ctx with one offering lookup — 134-case matrix; _bind_page merges bind GET/400-repaint; _login_page; services try/except dropped to process_exception; LOGGING block dropped — failed-login warning + 500 tracebacks survive via lastResort; settings literals compacted); app.css 171→75 (comments/blank-lines/multi-decl rules one-lined, .binding-creds/.spin/.form label code/.btn:disabled dead-or-DaisyUI rules dropped, drawer-hint opacity→fixed gray AA fix); sm_tags 14→5; mechanical blank-run collapse + 14 def-joins across all Python (script asserts AST-identity per file); caught+fixed SessionBase.update kwargs 500; identity 16/16 + check + 30/30 smoke + .gz branch exercised green |
| 2026-09-15 | 2034 | -343 | b36e495 | fan-out round 4 (3 agents, two verification phases): authz.py/oidc.py/records.py merged into views.py (8 files removed total incl. 5 templates) — 16-section identity snapshot byte-identical across the moves (offeringId dead keys dropped from SCHEMAS/BIND_SCHEMAS, PLAN_VALUES as verified comprehension); kube 239→214 + input_schema 209→177 + memory 118→111 comment/docstring compression (webhook-panic histories kept as one-liners); views banner purge + _page() render helper + decorator one-liner views; drawer_head/schema_modal/events_card/bind_form/credentials_error inlined, provision_form 16→8; app.js 63→50 (40-case smValidateParams battery old==new); docs re-wrapped ≤120 cols (REQUIREMENTS 76, token-identical; design 144; architecture 50 with merge-contingency table); check + identity + 30/30 smoke green |
| 2026-09-15 | 1816 | -218 | bb05b61 | fan-out round 5 (3 agents, three phases): memory 111→70 + input_schema 177→155 + kube 214→170 data-literal densification (STATIC_CATALOG/SCHEMAS/PLAN_VALUES/CR bodies one-lined — order-preserving json.dumps equality + 9-scenario kube stub parity); views 698→644 (_checked validate helper, lazy ternary backend dispatch, delete-verb ternaries, pkce/exchange_code/_precedence_ctx inlined, _cr_state CR preamble, ISSUER fold, dispatch-only decorators dropped, instance_edit as dispatch lambda, dead plan-fragment offering lookup); settings 51→37 (TEMPLATES one-liner, WSGI_APPLICATION + cookie defaults dropped — Django 5.2.6 defaults verified) + urls 30→23 (comprehension route groups); docs: design ASCII 38→25 (all 7 archetypes kept), README RBAC yaml comments (7 rules byte-identical), architecture table/dep-block merged, Dockerfile ENV folded; identity 16/16 (HEAD-rebased baseline via stash) + check + 30/30 smoke green |
| 2026-09-15 | 1483 | -333 | 2d544da | fan-out round 6 (3 agents; the round's true saving -333, the commit message's -N was a placeholder): engine-verified `{% extends %}{% block %}` same-line joins across 11 templates (templates 88→52) + README RBAC yaml → flow style (pyyaml parse-identity proven, 7 rules) + intro/Keycloak/Docker trims (103→80); views 644→515 (single-caller inlines: _plan_props/_free_input_hints/_contract_json/_bind_free_inputs_hint/_instances_table_ctx/_instance_detail_ctx folded; catalog_from_crs as one comprehension; record status chains as ternaries; 24 mechanical if/call joins) + middleware 65→45 (PUBLIC_PREFIXES folded, if-joins); kube 170→149 + input_schema 155→126 + memory 70→66 + settings 37→27 + urls 23→15 + manage/wsgi 6→4/3 (statement joins, walrus); app.js 50→35 (safe joins only — two ASI-unsafe proposals rejected) + app.css 71→56 (adjacent-rule joins); design.md 127→108 (dup-cut); battery 40/40 + identity 16/16 (stash-rebased) + check + 30/30 smoke green |
| 2026-09-15 | 1187 | -296 | 296447b | fan-out round 7 (3 agents): tokenizer-safe blank-line purge across ALL Python incl. function bodies (-140, AST-identity asserted per file); kube 120→88 (space/semi/fold join ops + GROUP,VERSION tuple + _name_or_slug ternary, comment folds); views 448→409 (10 single-line defs, auth_callback/daisyui joins, POST preambles ×3, hform removed after proving form_schema_for plan-independence for hints, edit/bind branch ternary collapses); templates 52→31 (engine-precedent extends+content joins); app.css 56→28 (pair-joins), app.js 35→30 (blank purge + 3 ASI-safe joins, battery 40/40); pyproject deps one-line (uv lock --check OK), Dockerfile 20→13, .gitignore globs, design 108→101, README 80→77, architecture 42, public/README curl &&-chains; identity 16/16 + check + 30/30 smoke green |
| 2026-09-15 | 805 | -382 | fe5e163 | fan-out round 8 (2 agents; backend agent delivered ast+battery-verified drop-in files, extracted programmatically from its output to avoid transcription risk): views 409→215 + input_schema 112→42 + memory 49→25 + middleware 39→18 + urls 14→1 + settings 21→7 + sm_tags 5→3 + manage 4→2 + wsgi 3→1 (statement joins, walrus-if merges, comment folds; agent's 32-check views battery + SCHEMAS/validators byte-identity probes; my 16-section identity snapshot re-verified via stash rebase); frontend floor: templates 31→16 (six files to single lines, instance_page 6→3), app.js 30→23 (7 brace-safe joins; battery extractor upgraded to brace-scan, 40/40), app.css 28→13 (triple-joins ≤600 chars), design.md 104→101 (Button+Alert/Card+Table row merges, chrome trim, opacity .55→0.6 accuracy fix); check + identity + battery + 30/30 smoke green |
| 2026-09-15 | 731 | -74 | 9782888 | fan-out round 9 (2 agents, drop-in files extracted from agent output): kube 88→52 (_init inlined as tuple-index expression, resolve_namespaced_refs ternary+walrus, list_events/tenant_namespaces one-liners — 61/61 stub scenarios old==new), views 215→199 (walrus guard-merges in auth_local/auth_callback/_public_file/schema_download/instance_show/instance_edit_post/binding_delete, _checked res-index, provision_form_fragment one-liner — 80/80 differential battery), input_schema 42→36 (PLAN_VALUES close join, required folded into the dict literal — 120/120 form_schema + 276/276 validator probes), memory 25→24 (_delete walrus guard), middleware 18→16 (session walrus + tenant fold — 13-path battery); app.js 23→14 (9 ASI-audited `;` joins, node vm.Script-verified, battery 40/40), instance_page 3→1 (3925-char line, whitespace-normalized identical), design 101→99 (Principles trim + Modal/Spinner merge — honest correction from the estimated 98); identity 16/16 (stash-rebased) + check + 30/30 smoke green |
| 2026-09-16 | 656 | -72 | 5ead2c7 | fan-out round 10 (2 agents; the floor verdict was CHALLENGED and OVERTURNED — sha256-verified drop-ins): views 199→160 (for-loops→{**r,…}/dict(r,…) copy-comprehensions proven non-polluting, guard-ifs→ternaries, 3 decorator→require_GET(lambda) assignments, _offering_ctx hints as starred-unpack nested comprehension — per-function differential probes incl. 25/25 hints), kube 52→41 (L1-6 docstring+imports+_in_cluster+kube as one blob, ensure_namespace/create_instance one-liners — 19/19 stub parity), input_schema 36→31 (form_schema_for loop→3 comprehensions — 50/50 matrix byte-identical), memory 24→23, middleware 16→14 (setattr fold), settings 7→6; README RBAC yaml→whole-doc flow mapping (pyyaml parse-identity, 7 rules), public/README curl→single &&-chain + first-party table rows merged, design 99→97 (Inputs+Drawer, DefList+CodeChip), architecture 42→41, app.css 13→12; identity 16/16 + battery 40/40 + check + 30/30 smoke green |
| 2026-09-16 | 569 | -87 | 11156a1 | fan-out round 11 (2 agents): descriptive docs compressed to core — design.md 97→36 (ASCII archetype diagrams→terse 7-item list, tokens packed onto 6 dense lines, inventory→4-line list; keep-list proves all 16 hexes + 5.49:1/6.35:1/≥4.5:1 + 720px + every measured size survived; overlay-permanence rationale kept for the public/README pointer), architecture.md 41→21 (responsibility table merged to 6 rows, dependency graph compacted, invariants→4 merged bullets); REQUIREMENTS.md 76→70 pure-text tightening — 6 edits each with a no-weaken proof (docs bullet, IBR paragraph, vendored-assets sentence, items 1/9/10; item 11 var list verbatim untouched; 13 env vars + all pinned tokens grep-verified); check + 30/30 smoke green |
| 2026-09-16 | 566 | -3 | 63cfa98 | final codebase review (owner Clean Code manifesto; 3 agents: review + AI/contextual trace scan + file-count audit, fixes applied directly): YAGNI purge — dead `bindInputSchema` key, binding_record `reason`/`message` keys no template reads (also §7 parity), dead `.replace('Z',...)` on pinned py3.12, unreachable `_aged` guards, `_event` warn param; input_schema SCHEMAS pinned userInputs entries existed only to be filtered out — deleted with all now-dead filters (form_schema_for full matrix byte-identical, drawer_contract loses only the 2 unreachable user entries, pinned rejection map intact); plan-fields comprehension deduped into `_plan_fields()`; intent comments restored on the kube static-catalog fallback and the provision-vs-edit error-policy asymmetry; django-htmx dependency dropped (single truthiness check → documented `HX-Request` header, 6→5 deps, uv lock regenerated); contextual traces removed (Stratos pattern comment, origin-repo pointer, private tailnet default → keycloak.example.com, stale Where column → core/views.py, BFF→server ×5, design.md Service-detail archetype REQUIREMENTS forbids + menu-label/250px drift); .zcode plan artifact + stale pycache deleted; CJK rescan ZERO, battery 40/40, check + 30/30 smoke + no-header 302 redirect green; 39 tracked files, all types covered |
| 2026-09-16 | 566 | 0 | 8703abf | full kitchen-sink scan round (ruff/bandit/vulture/radon/mypy + gitleaks/detect-secrets/pip-audit + djlint/tinycss2/hadolint/codespell + radon MI; 3 agents): CRITICAL fix — daisyui_css returned a (None,None) tuple on the gzip path (the round-10 `and/or` compression trick; every browser → 500 unstyled UI; caught because curl never sends Accept-Encoding) — rewritten as the clean flat if, gzip branch now a permanent smoke assertion (31/31); lint fixes (dead password walrus, raise-from-None, direct return in form_schema_for); bandit sha1 silenced via usedforsecurity=False; H021 inline styles → app.css rules (0 net); Dockerfile EOF newline; deps Django 5.2.6→5.2.17 + requests 2.32.5→2.33.0 — pip-audit: No known vulnerabilities; admin_tenants/_ready_condition inlined + kube regex constants joined to offset the +3-line clean fix (net 0, log monotonic); ruff re-scan clean, bandit 0, check + battery 40/40 + 31/31 smoke green |
| 2026-09-16 | 3010 | +2444 | cd825c7 | OWNER DIRECTIVE (ends the compression style): full-repo reformat with canonical formatters — ruff format (black-compatible, 88-col) on all Python, prettier on app.js/app.css/all markdown, djlint --reformat (django profile) on all 14 templates. The single-line/semicolon/walrus-chain era is over; formatter output is the house style from here. Behavior verified identical after reformat: all 14 templates compile through the real engine, node --check, manage.py check, smValidateParams battery 40/40, full smoke 31/31 incl. the gzip branch. Both formatters idempotent (ruff format --check and prettier --check clean). This row intentionally breaks the monotonic-decrease rule by owner instruction; the 566-line state is preserved at commit 8703abf^..45337af if ever needed. |
| 2026-09-16 | 3021 | +11 | 8e50713 | OWNER DIRECTIVE (mock separation): kube mode no longer falls back to the dev STATIC_CATALOG — the cluster catalog is shown as-is, empty means an honest empty marketplace (removes the S3 on-call trap: fake tiles that 400 on provision); the in-memory backend is now visibly labeled — full-width "Demo backend" banner on every page (incl. login) + cluster chip reads "demo · in-memory" when kube is unconfigured; _page_ctx gains a demo flag; smoke asserts the banner. Follow-up b7afa17: kube.py lambda→def + E401 import split — ruff E,F,W fully clean under the new formatter house style. check + battery 40/40 + smoke 31/31 green. |
| 2026-09-16 | 3028 | +7 | 7ed6218 | OWNER CHALLENGE (test the real cluster, not just the mock): kube.py gains a kubeconfig fallback (load_incluster_config except ConfigException → load_kube_config) so a dev box with KUBE_IN_CLUSTER=true uses the current kubectl context — the README's "dev against a port-forwarded cluster" now actually works. FIRST-EVER real-cluster verification, end-to-end on MINT: real catalog (postgresql/redis/vm from cloud-service-broker), provision e2e-real redis/small → broker namespace → Ready via the real broker, plan/params edit 302, binding → Ready with real secret, credentials reveal decoded a REAL host/port/password from the K8s secret, delete binding + instance → resources fully deprovisioned (verified with kubectl). Both backends now provably work: memory smoke 31/31 + this real-cluster e2e. Found + fixed en route: nothing code-side (the two e2e stumbles were test-script bugs — Location-header capture and the §2 rule that local admin in kube mode sees live namespaces, so ?tenant=svccat-e2e was rejected and fell back to `broker`). |
| 2026-09-17 | 4127 | +1099 | 5ed4571 | OWNER BAR (readability + test safety-net ≥8/10, scored 2 and 1-2 by the ops-persona audit): committed a 94-test suite (tests/ — memory e2e incl. every 400 path, input-contract byte expectations + JS-mirror battery via node, tenancy/records/slugs, memory-store lifecycle, mocked-kube write paths incl. pinned-params-LAST dict order + RoleBinding 404-create sequence + resolve-refs, middleware gate anon/tenant-rejection/no-store, every page+fragment GET render, OIDC PKCE/state-mismatch/jwt, gzip variant + clean-404) and de-clevered every audit-flagged construct (status ternary chains → if/elif ladders, all lambda views → decorated defs, _dispatch inner def, side-effect ternaries → if/else, walrus-ternaries → helpers, comprehension let-bindings → loops incl. catalog_from_crs inner loop + PLAN_VALUES + kube list_events/_enrich, schema_download 10-line ternary → guard+build). Independent re-audit verdict: readability 8/10 MET, safety-net 8/10 MET (94/94, 0 skips; named gap: update_instance at zero tests). manage.py test OK, ruff E,F,W + format clean, smoke 31/31, battery 40/40. |
| 2026-09-17 | 4129 | +2 | b64f433 | triple review round (3 agents: architecture re-review @4127-line scale, ops re-audit + full 14-scanner rerun, HTTP parameter-placement audit): scanners 12/14 fully clean (codespell clean excl. vendored min.js; ruff-format clean; bandit/gitleaks/pip-audit/djlint/tinycss2/hadolint/CJK zero) — three real defects fixed (dead aged_record test helper, manage.py E401, kubeconfig fallback now logs the resolved context — the dev-box-into-prod trap made loud); architecture: 6-row model holds, 10 drift items found in architecture.md (stale claims: middleware imports views, session read by layout, tests layer missing from the doc, kubeconfig 3-mode seam undocumented) + split recommendation (records.py out of views pre-endorsed by history); API audit verdict: path/query/body placement correct except tenant-scope-in-query (K8s-native would be /ns/<tenant>/...), param naming inconsistent (6 spellings for 3 concepts), logout-is-GET verb violation — three ranked recommendations presented to owner (rename XS, tenant-in-path M, logout POST XS); 94/94 + smoke 31/31 green |
| 2026-09-17 | 4136 | +7 | 9847739 | docs cleanup round: contextual traces removed (PCF lineage ref dropped from design.md, services-only info kept), architecture.md rewritten against the 10-item drift list (middleware→views edge documented, session-read-by-layout acknowledged, validation split stated, app.js contract path corrected to views._contract_json, kubeconfig 3-mode seam documented, tests/ row added with import+mock policy), README KUBE_IN_CLUSTER row now documents the kubeconfig fallback; K8s production-readiness review also delivered this round (healthz/readyz gap, gunicorn timeout flags, RBAC 6 unused verbs, no Deployment manifest — fixes pending owner sanction); trace+CJK rescan clean (this log excepted by design); check + 94/94 tests + prettier green |
